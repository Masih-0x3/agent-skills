interface:
  display_name: "Obscura Browser MCP"
  short_description: "Default browser labor through Obscura MCP"
  default_prompt: "Use $obscura-browser-mcp as the default worker browser, escalating to Chrome only for visual or profile-specific proof."
dependencies:
  tools:
    - type: "mcp"
      value: "obscura"
      description: "Local Obscura MCP browser server"
      transport: "stdio"
